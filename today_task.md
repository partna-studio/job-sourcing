1. Turn the Resume Path into a file upload (accepts docx)
2. Enable Git Action to happen in 3 ways:
   1. Via Scheduler (Weekly for all available (subscribed??) users)
   2. Via API (on-demand trigger) (Always check if weekly data exist first before re-running)
   3. Via Extension (Always check if weekly data exist first before re-running)
3. Connect all neccessary firebase collections in google sheets
4. Build Web App Screens
   1. Dashboard
   2. Not Connected to Linkedin Screen
   3. Data Manipulation Screen
   4. First-Time Login Form (pop-up)
   5. Way to edit form submission
